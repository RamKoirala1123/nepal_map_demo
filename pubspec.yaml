name: nepal_map_example
description: Example app demonstrating the nepal_map package.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  nepal_map:
    git:
      url: https://github.com/RamKoirala1123/nepal_map_flutter.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
